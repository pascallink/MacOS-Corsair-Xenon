{
  "name": "xeneon-edge-macos",
  "private": true,
  "scripts": {
    "lint:commits": "commitlint --from origin/develop --to HEAD --verbose"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.2",
    "@commitlint/config-conventional": "^21.2.2"
  }
}
